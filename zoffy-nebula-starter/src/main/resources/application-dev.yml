spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/zoffy
    username: postgres
    password: 1qaw3@123
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace